version: '2'

services:

  shortcodes-portal-uat:
    container_name: shortcodes-portal
    restart: always
    build:
      context: .
      dockerfile: DockerFile
    image: shortcodesui:v1
    ports:
      - '8084:80'